
% W1: Exercise (Video): Basics of Node.js and NPM -----
npm init
% follow questions ... makes package.json
npm install lite-server --save-dev
% to install lite-server and save it to use in the project -dev for module from 
% which my project directly dependent. and will be included in "devDependencies"
% in "scripts" add "start" - will run things ... 
"start": "npm run lite",
. . . . . .
"lite": "lite-server"   % <- looks like aliace?!?!
%----------
npm start   % <- to start in cmd
%--------------------------------------------------------
%   Introduction to Bootstrap: Exercise (Video): Getting Started with Bootstrap
% In conFusion :
npm install           % <- will install the lite-server

npm install bootstrap@4.0.0 --save
npm install jquery@3.3.1 popper.js@1.12.9 --save    % <- will install bootstrap & jquery

% Insert the following code in the <head> of index.html file before the title.
<!-- Required meta tags always come first -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta http-equiv="x-ua-compatible" content="ie=edge">

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="node_modules/bootstrap/dist/css/bootstrap.min.css">

% At the bottom of the page, just before the end of the body tag
<!-- jQuery first, then Popper.js, then Bootstrap JS. -->
    <script src="node_modules/jquery/dist/jquery.slim.min.js"></script>
    <script src="node_modules/popper.js/dist/umd/popper.min.js"></script>
    <script src="node_modules/bootstrap/dist/js/bootstrap.min.js"></script>

%   Introduction to Bootstrap: Exercise (Instructions): Getting Started with Bootstrap
https://marketplace.visualstudio.com/items?itemName=ecmel.vscode-html-css  <- set extension for VS-code

% You can get Font Awesome using npm by typing the following at the prompt:
npm install font-awesome@4.7.0 --save
% Bootstrap Social that enables the addition of Social buttons to our site:
npm install bootstrap-social@5.1.1 --save
% to include the CSS files for font awesome and bootstrap-social in the index.html :
    <link rel="stylesheet" href="node_modules/font-awesome/css/font-awesome.min.css">
    
Less & Sass ----------------
Less:
>> npm install -g less@2.7.2
Next type the following at the command prompt to compile the Less file 
into a CSS file:
>> lessc styles.less styles.css

Sass:
>> npm install --save-dev node-sass@4.7.2
Next open your package.json file and add the following line into the scripts
object there.  This adds a script to enable the compilation of the Scss file
into a CSS file:
    "scss": "node-sass -o css/ css/"
In order to transform the Scss file to a CSS file, type the 
following at the prompt:
>> npm run scss

